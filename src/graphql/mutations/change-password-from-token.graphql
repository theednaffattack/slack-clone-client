mutation ChangePasswordFromToken($data: ChangePasswordInput!) {
  changePasswordFromToken(data: $data) {
    errors {
      field
      message
    }
    user {
      id
      name
      username
    }
  }
}
